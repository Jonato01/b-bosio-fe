export const environment = {
  production: true,
  // On Azure, prefer a relative path so that the same origin is used.
  // If your backend is hosted on a different domain, replace with the full URL.
  apiUrl: ' https://agenda-db-hrg9hnercub4achp.francecentral-01.azurewebsites.net/api'
};

